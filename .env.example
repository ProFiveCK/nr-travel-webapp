# ============================================================================
# NR Travel Webapp - Environment Configuration
# ============================================================================
# 
# This file controls all environment-specific settings.
#
# DEPLOYMENT MODES:
#   1. Development: Local development with Vite dev server
#      → Run: npm run dev (backend & frontend separate)
#   
#   2. Docker: Containerized with Docker Compose
#      → Run: docker compose up -d
#      → Access: http://localhost:8090
#   
#   3. Production: External deployment
#      → Set PUBLIC_URL to your actual domain
#      → Use strong secrets (JWT_SECRET, REFRESH_SECRET, DB passwords)
#
# TIP: Use ./setup-config.sh to auto-configure for your environment
# ============================================================================

# Environment
NODE_ENV=development

# ============================================================================
# SECURITY - CHANGE THESE FOR PRODUCTION!
# ============================================================================
JWT_SECRET=change-me-to-a-strong-secret-value
REFRESH_SECRET=change-me-to-another-strong-secret-value

# ============================================================================
# FRONTEND & API URLs
# ============================================================================
# For development (local):
CLIENT_URL=http://localhost:5173
API_URL=http://localhost:4000
VITE_API_URL=http://localhost:4000

# For Docker:
# CLIENT_URL=http://localhost:8090
# API_URL=http://localhost:8090
# VITE_API_URL=/api

# For production:
# CLIENT_URL=https://travel.yourdomain.com
# API_URL=https://travel.yourdomain.com
# VITE_API_URL=/api

# ============================================================================
# DATABASE - PostgreSQL
# ============================================================================
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_secure_db_password
POSTGRES_DB=travel
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# ============================================================================
# STORAGE & UPLOADS
# ============================================================================
UPLOADS_DIR=/app/uploads

# ============================================================================
# CACHE - Redis
# ============================================================================
REDIS_URL=redis://redis:6379

# ============================================================================
# EMAIL - Zoho SMTP (or your mail provider)
# ============================================================================
SMTP_HOST=smtppro.zoho.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USERNAME=your-email@domain.com
SMTP_PASSWORD=your-app-specific-password
SMTP_FROM=Travel Desk <your-email@domain.com>

# ============================================================================
# PUBLIC URL - For external access / SSL certificates
# ============================================================================
# Used for generating external links, SSL cert references, etc.
PUBLIC_URL=http://localhost:8090
# For production: PUBLIC_URL=https://travel.yourdomain.com
